{
  "name": "luncheon-lindholmen-mvp",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": ">=18.18.0"
  },
  "scripts": {
    "dev": "next dev",
    "clean": "rm -rf .next",
    "dev:clean": "npm run clean && next dev",
    "dev:lan": "next dev -H 0.0.0.0",
    "build": "next build",
    "start": "next start",
    "reinstall": "rm -rf node_modules .next && npm install",
    "lint": "node -e \"console.log('ESLint removed from npm install (Vercel). Use editor lint or add eslint locally if needed.')\"",
    "check": "npm run build"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.0.2",
    "@types/leaflet": "^1.9.21",
    "@types/node": "^20.11.0",
    "@types/react": "^18.2.0",
    "@types/react-dom": "^18.2.0",
    "date-fns": "^4.1.0",
    "date-fns-tz": "^3.2.0",
    "leaflet": "^1.9.4",
    "next": "^16.2.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-leaflet": "^4.2.1",
    "typescript": "^5.4.0"
  }
}
